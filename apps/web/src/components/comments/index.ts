/**
 * Comments Components
 * Unified comments UI components for CRM entities
 *
 * @module components/comments
 */

// Main orchestrator
export { CommentSection, default as CommentSectionDefault } from './CommentSection';

// Individual components
export { CommentCard, default as CommentCardDefault } from './CommentCard';
export { CommentInput, default as CommentInputDefault } from './CommentInput';
export { ReactionBar, default as ReactionBarDefault } from './ReactionBar';
